# Versions are pinned to the working local .venv (Python 3.10) so that pip
# inside Docker installs the exact same set with no dependency backtracking.
# To upgrade: bump the pin locally, verify, then rebuild the image.

# Core ML & NLP
# CPU-only torch wheels: the default Linux wheel bundles ~2.5GB of CUDA libs
# that the container can never use. Remove the index line to get the GPU build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
transformers==5.16.1
datasets==5.0.1
accelerate==1.14.0
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
pyarrow==25.0.1

# Experiment Tracking & Model Registry
mlflow==3.15.2

# Feature Store
feast==0.66.0

# Pipeline & DAG Orchestration
prefect==3.8.4

# API & Serving
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5

# Monitoring, Drift Detection & Dashboard
evidently==0.7.21
streamlit==1.62.0
plotly==5.24.1

# Testing & Utilities
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
tqdm==4.70.0
requests==2.34.2
